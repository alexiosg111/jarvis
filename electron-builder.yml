appId: "com.jarvis.hub"
productName: "Jarvis Hub"
directories:
  output: "release"
files:
  - "dist/**"
  - "package.json"
mac:
  target:
    - "dmg"
  category: "public.app-category.productivity"
  artifactName: "${productName}-${version}-mac-intel.${ext}"
win:
  target:
    - "nsis"
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
linux:
  target:
    - "AppImage"
